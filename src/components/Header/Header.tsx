import React from 'react'
import s from './Header.module.scss'

export const Header = () => {
    return <header className={s.title}>
        Simple Hotel Rooms Check
    </header>
}

export default Header